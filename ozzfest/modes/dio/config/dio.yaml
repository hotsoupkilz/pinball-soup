#config_version=6

mode:
  start_events: start_dio_mode
  stop_events: stop_dio_mode, dio_mode_completed
  priority: 310

variable_player:
  # Baseline major shots during Dio (keeps bands consistent)
  shot_lord_l_lit:
    score: 2500
  shot_lord_o_lit:
    score: 2500
  shot_lord_r_lit:
    score: 2500
  shot_lord_d_lit:
    score: 2500

  # Focus area: loops/turnarounds get a bigger bump
  s_turnaround_lower_rollover_active:
    score: 3000
  s_turnaround_upper_rollover_active:
    score: 3000
